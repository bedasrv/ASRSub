# ASRSub v2 — Parallel Track Contract (2026-08-07)

Repo: `~/benchmark/pipeline` on PC (user@10.10.20.208), origin `bedasrv/ASRSub`.
Base: `main` @ f7394af. Three parallel worktrees, each on its own branch, all
merge into `main` when their track is done and verified.

## Tracks

| Track | Branch / worktree | Owns (may create/edit) | Must NOT touch |
|-------|-------------------|------------------------|----------------|
| eval | `eval` / `~/benchmark/pipeline-eval` | `benchmarks/` (new dir), `requirements-eval.txt` (new), own venv `~/benchmark/venvs/eval` | orchestrator.py, one_shot_test.py, dashboard.*, pctl, control_api_v2.py |
| rewrite | `rewrite` / `~/benchmark/pipeline-rewrite` | orchestrator.py, `pipeline/` (new modules), one_shot_test.py (may move/supersede), PLAN.md updates | dashboard.py, dashboard.html, pctl, benchmarks/ |
| dashboard | `dashboard` / `~/benchmark/pipeline-dashboard` | dashboard.py, dashboard.html, pctl, `control_api_v2.py` (NEW module) | orchestrator.py (integration is rewrite track's job), one_shot_test.py, benchmarks/ |

## Frozen cross-track contract

1. **control_api_v2.py** (dashboard track creates): new module exposing expanded
   control/telemetry endpoints. Public surface: class `ControlAPIv2(cfg)` with
   `.endpoints` dict path→handler, plus `.register(server)` attaching handlers to
   a ThreadingHTTPServer. Dashboard track defines it and can change it freely;
   rewrite track integrates it in orchestrator.py (register alongside existing
   ControlHandler) once dashboard track reports it stable. If rewrite merges
   before dashboard is done, rewrite keeps existing ControlHandler untouched.
2. **AI suffix naming** (proposal, final call = rewrite track after research):
   SRT written as `<base>.<lang>.ai.srt` + first line `[AI-generated by ASRSub]`.
   Bazarr picks language from the `.id` token; `.ai` marks provenance. Dashboard
   shows the AI marker; eval does not care.
3. **SDH placeholders**: keep the `(Lirik lagu)` style. Rewrite may extend the
   placeholder set (e.g. `(SFX)`) only as a config-driven list, never hardcoded.
4. **Public interfaces rewrite track must preserve** (dashboard/pctl depend on
   them): `load_config()`, `post_chat()`, `chat_translate_batch()`,
   `halt_on_error()`, `guard_foreign_lines()`, `sanitize_lines()`, `write_srt()`,
   control API routes `/status /config /pause /resume /run-once /wake /health`,
   env keys in `~/.config/asr-pipeline/pipeline.env` (BAZARR_URL, BAZARR_API_KEY,
   SONARR_URL, SONARR_API_KEY, ASR_URL, TRANSLATE_BASE, TRANSLATE_MODEL,
   CONTROL_API_KEY, TARGET_LANGS, MAX_EPS_PER_RUN). Signatures may change ONLY
   with a note in the merge commit.
5. **Never commit**: pipeline.env, state.jsonl, refine_state.jsonl, last_error.json,
   error_history/, __pycache__, .venv, selfheal_fix_spec_r*.md.

## Per-track goals

- **eval**: (a) ja ASR fidelity on ReazonSpeech (WER/CER, faster-whisper
  lv3-turbo int8 + VAD vs reference), (b) MT quality FLORES-200 jpn_jpan→ind_Latn
  (sacrebleu spBLEU + COMET if runnable, else report), (c) LLM-judge
  naturalness/fidelity on the D×D S03E07 SRT sample. Output: `benchmarks/README.md`
  with commands + results table. Must run on the PC (GPU for whisper).
- **rewrite**: production orchestrator v2. Adopt from one_shot_test.py:
  faster-whisper lv3-turbo int8 + Silero VAD (ja, condition_on_previous_text=False),
  merge-aware translate (chunk 10 + tail completion), tolerant `N…` parser.
  Add: whisper segment splitting (fix 106s merged segments — split long segments
  on internal silence/VAD boundaries), per-series name glossary (config file
  `~/.config/asr-pipeline/glossary.json` with en→ja/romaji maps injected as
  translation REF lines), AI suffix naming above, SDH placeholder config.
  Keep: daemon loop + flock, webhook wake, halt-on-error, adaptive sleep,
  state.jsonl, control API. Target: D×D S03E07 re-run equals one_shot output
  quality with 0 timing violations and ≤2 empty cues.
- **dashboard**: v2 UI, single page, collapsible sections (user preference —
  no nav bars/tabs/hamburger). Informative+interactive: live per-episode
  progress, llama-server health + nvidia-smi VRAM/GPU stats, queue depth,
  per-episode retry/skip actions, recent activity table, wanted list with
  quality flags, AI-marker display, config editor + pause/resume/run-once/wake
  (existing behavior must keep working). New endpoints in control_api_v2.py.

## Merge order

dashboard and eval merge independently when green. rewrite merges last
(integrates control_api_v2.py if present). Merge = `git checkout main && git
merge <branch>` + push + `herdr worktree remove` on clanker side.
