# ASRSub v2 — Parallel Track Contract (2026-08-07)

Repo: `~/benchmark/pipeline` on PC (user@10.10.20.208), origin `bedasrv/ASRSub`.
Base: `main` @ f7394af. Three parallel worktrees, each on its own branch, all
merge into `main` when their track is done and verified.

## Tracks

| Track | Branch / worktree | Owns (may create/edit) | Must NOT touch |
|-------|-------------------|------------------------|----------------|
| eval | `eval` / `~/benchmark/pipeline-eval` | `benchmarks/` (new dir), `requirements-eval.txt` (new), own venv `~/benchmark/venvs/eval` | orchestrator.py, one_shot_test.py, dashboard.*, pctl, control_api_v2.py |
| rewrite | `rewrite` / `~/benchmark/pipeline-rewrite` | orchestrator.py, `pipeline/` (new modules), one_shot_test.py (may move/supersede), PLAN.md updates | dashboard.py, dashboard.html, pctl, benchmarks/ |
| dashboard | `dashboard` / `~/benchmark/pipeline-dashboard` | dashboard.py, dashboard.html, pctl, `control_api_v2.py` (NEW module) | orchestrator.py (integration is rewrite track's job), one_shot_test.py, benchmarks/ |

## Frozen cross-track contract

1. **control_api_v2.py** (dashboard track creates): new module exposing expanded
   control/telemetry endpoints. Public surface: class `ControlAPIv2(cfg)` with
   `.endpoints` dict path→handler, plus `.register(server)` attaching handlers to
   a ThreadingHTTPServer. Dashboard track defines it and can change it freely;
   rewrite track integrates it in orchestrator.py (register alongside existing
   ControlHandler) once dashboard track reports it stable. If rewrite merges
   before dashboard is done, rewrite keeps existing ControlHandler untouched.
2. **AI provenance naming** (SETTLED — rewrite track, verified 2026-08-07):
   AI provenance is a REAL first SRT cue `00:00:00,000 --> 00:00:01,500`
   `[AI-generated by ASRSub]` (config `AI_MARKER_CUE`, default on; marker end
   bumps to just before the first dialogue cue when it starts < 2s). Bare
   header lines are skipped by players (ffmpeg srtdec.c, VLC subtitle.c) and
   SRT has no comment syntax. On-disk files are plain `{stem}.{lang}.srt`
   (no `.ai` suffix): Bazarr ignores uploaded filenames anyway (subliminal
   writes `{video stem}.{lang alpha2}.srt`), and `.ai` in filenames would only
   confuse content-sniffing. Known limitation (guessit): Indonesian `id` is
   NEVER auto-detected (parses as episode_title) — Bazarr's existing-file scan
   falls back to content-sniffing, which is expected; do NOT add `.ai` back as
   a workaround. Upload provenance metadata: none — the Bazarr upload
   endpoints have no comment/history field (verified in live swagger.json).
3. **SDH placeholders**: keep the `(Lirik lagu)` style. Rewrite may extend the
   placeholder set (e.g. `(SFX)`) only as a config-driven list, never hardcoded.
4. **Public interfaces rewrite track must preserve** (dashboard/pctl depend on
   them): `load_config()`, `post_chat()`, `chat_translate_batch()`,
   `halt_on_error()`, `guard_foreign_lines()`, `sanitize_lines()`, `write_srt()`,
   control API routes `/status /config /pause /resume /run-once /wake /health`,
   env keys in `~/.config/asr-pipeline/pipeline.env` (BAZARR_URL, BAZARR_API_KEY,
   SONARR_URL, SONARR_API_KEY, ASR_URL, TRANSLATE_BASE, TRANSLATE_MODEL,
   CONTROL_API_KEY, TARGET_LANGS, MAX_EPS_PER_RUN). Signatures may change ONLY
   with a note in the merge commit.
5. **Never commit**: pipeline.env, state.jsonl, refine_state.jsonl, last_error.json,
   error_history/, __pycache__, .venv, selfheal_fix_spec_r*.md.

## Per-track goals

- **eval**: (a) ja ASR fidelity on ReazonSpeech (WER/CER, faster-whisper
  lv3-turbo int8 + VAD vs reference), (b) MT quality FLORES-200 jpn_jpan→ind_Latn
  (sacrebleu spBLEU + COMET if runnable, else report), (c) LLM-judge
  naturalness/fidelity on the D×D S03E07 SRT sample. Output: `benchmarks/README.md`
  with commands + results table. Must run on the PC (GPU for whisper).
- **rewrite**: production orchestrator v2. Adopt from one_shot_test.py:
  faster-whisper lv3-turbo int8 + Silero VAD (ja, condition_on_previous_text=False),
  merge-aware translate (chunk 10 + tail completion), tolerant `N…` parser.
  Add: whisper segment splitting (fix 106s merged segments — split long segments
  on internal silence/VAD boundaries), per-series name glossary (config file
  `~/.config/asr-pipeline/glossary.json`), AI suffix naming above, SDH
  placeholder config. Keep: daemon loop + flock, webhook wake, halt-on-error,
  adaptive sleep, state.jsonl, control API. Target: D×D S03E07 re-run equals
  one_shot output quality with 0 timing violations and ≤2 empty cues.

  **Glossary mechanism (DECIDED — HY-MT1.5 official terminology intervention)**
  Per HY-MT1.5 Technical Report §3.4 Scenario 1, use the model's natively
  trained terminology template instead of ad-hoc `REF:` lines:
  `参考下面的翻译：\nアーシア翻译成Asia；イッセー翻译成Issei；…` prepended to
  the numbered-chunk system prompt. Verified live on HY-MT1.5-7B (llama-server
  :8011, 2026-08-07): 0 terms → FAIL (cjk=[3], Lizevim drift); 5/10/15 terms →
  OK (10/10 parsed, 0 CJK echo, 0 template echo, Rizevim correct). The report's
  Scenario 3 formatted-translation template (`<s1>…</s1>` wrapped in
  `<target>…</target>`) was ALSO tested and REJECTED: CJK leak (Dio德拉) and
  worse romanization drift (Arshia/Issae/Lias) — it does not carry the
  glossary, so the numbered protocol stays as the structure contract.
  `pipeline/probe_templates.py` + `pipeline/probe_refs.py` hold the evidence.
  Test output (D×D S03E07, official template): SMOKE PASS — 344 cues, 0 timing
  violations, 0 empty, 0 CJK, 0 >8s, first cue `[AI-generated by ASRSub]`,
  file `<base>.id.srt`, translate 86s.

  **VAD/segment-split corrections (deep-research round, 2026-08-07)**
  - `vad_parameters` is now a `faster_whisper.transcribe.VadOptions` INSTANCE
    (threshold 0.5, min_silence_duration_ms 200, max_speech_duration_s 8,
    speech_pad_ms 250; env-overridable). Passing a dict silently overrides
    `max_speech_duration_s` with `chunk_length` (30s default) — the 8s cap
    never applied. Kwargs are filtered to the dataclass fields available in
    the installed version (`min_silence_at_max_speech 98` only where the
    field exists; absent in faster-whisper 1.2.1). Fallback when VadOptions
    is missing: `chunk_length=8` + dict.
  - Contiguity post-pass over cues: `next.start = max(next.start, prev.end)`
    (never overlap).
  - Cue split threshold renamed `SPLIT_MIN_SILENCE_MS` (default 500) so the
    VAD `min_silence_duration_ms` (200) and the cue-cut gap are independent.
  - SDH placeholders sanity-checked: labels are plain text (never italicized),
    single line (< 2 lines/cue), no end punctuation — current default
    `["（歌詞）"]` already conforms; set unchanged.

  **Bazarr upload verified (live swagger, 2026-08-07)**
  `POST /api/episodes/subtitles` (query seriesid/episodeid/language/forced/hi
  + multipart `file`) is the manual-upload route (NOT `/api/subtitles`);
  movies use `POST /api/movies/subtitles` (radarrid) — not used (Sonarr-only).
  No comment/history field on upload → AI provenance lives only in the SRT
  marker cue.

  **SenseVoice backend (ASR_BACKEND=sensevoice; validated 2026-08-10)**
  SenseVoiceSmall + fsmn-vad as an alternative ASR backend, replacing the
  whisper path only when ASR_BACKEND=sensevoice (whisper path untouched).
  VAD recipe empirically validated on PC (Frieren S02E01 180s slice, funasr
  1.4.0, sensevoice venv; monster 17.5-21s spans → max 4.68s):
  `vad_kwargs={"max_single_segment_time": 5000, "max_end_silence_time": 400,
  "speech_pad_ms": 150, "min_speech_duration_ms": 250}` passed at AutoModel()
  CONSTRUCTION — never raw kwargs to inference() (crashes with
  chunk_size=None → frontend.fs NoneType). funasr 1.4.0 `res[0]["sentence_info"]`
  does NOT exist; the working path is generate(output_timestamp=True,
  merge_vad=False) + per-window char timestamps + bucket chunks into fsmn-vad
  windows. 400ms max_end_silence splits inside rapid dialogue (gaps 300-900ms)
  → 41% sub-1s fragments; neither the doc's 200ms rule nor _merge_tiny's
  0.6s-gap rule resolves them. Rewrite implements `_min_dur_postpass`:
  extend to 1.0s when the gap allows (≥150ms after extension), else merge
  with the closer neighbor (tie → previous) capped at 6.0s, else drop the
  fragment (last resort, unreachable in practice) — never emits <1s cues.
  SenseVoice language codes: ja/zh/en/ko/yue (ASR lang stays "ja").
  `pipeline/sensevoice.py` vendors the logic (main-worktree adapter is
  `~/benchmark/adapters/sensevoice.py`).

  **Jellyfin refresh + actions/exclusions (2026-08-10)**
  - After a successful upload AND after a delete action, a fire-and-forget
    Jellyfin library refresh runs (daemon thread, 5s timeouts, failures only
    logged, never fails the episode): GET /Items SearchTerm=episode title
    (fallback filename) with Fields=Path → match item whose Path equals the
    mapped media path (host prefix → JELLYFIN_MEDIA_ROOT) or ends with the
    video filename → POST /Items/{id}/Refresh with
    {"MetadataRefreshMode":"FullRefresh","ImageRefreshMode":"None",
    "ReplaceAllMetadata":false,"ReplaceAllImages":false} (204). Verified live
    on NAS 10.10.20.160:8096 (container jellyfin; realtime monitor off, no
    scan interval — refresh is required to pick up/drop subtitle files).
    Env keys: JELLYFIN_URL (default http://10.10.20.160:8096),
    JELLYFIN_API_KEY (empty = feature off), JELLYFIN_MEDIA_ROOT (default
    /media). No-op without the API key.
  - actions.jsonl (dashboard control_api_v2 schema: {"ts", "type":
    "retry"|"skip"|"delete", "episode_id", "language", "source", "note"},
    "action" accepted as type alias): consumed once per pass at run_pass
    start, then the file is truncated (atomic rewrite). skip → episode
    excluded from this pass's candidates; retry → done/error state entries
    cleared (language-limited when the record carries a language), so it
    re-processes; delete → all {video stem}.{lang}.srt files on the NAS path
    + TMP_DIR copies removed, done state cleared (regenerates), then a
    Jellyfin refresh drops the removed subtitle.
  - exclusions.jsonl ({"episode_id": N, ...}): parsed once per pass and
    filtered out of candidate selection before the MAX_EPS_PER_RUN cap is
    applied (excluded ids never consume the cap); logged once per pass.
- **dashboard**: v2 UI, single page, collapsible sections (user preference —
  no nav bars/tabs/hamburger). Informative+interactive: live per-episode
  progress, llama-server health + nvidia-smi VRAM/GPU stats, queue depth,
  per-episode retry/skip actions, recent activity table, wanted list with
  quality flags, AI-marker display, config editor + pause/resume/run-once/wake
  (existing behavior must keep working). New endpoints in control_api_v2.py.

## Merge order

dashboard and eval merge independently when green. rewrite merges last
(integrates control_api_v2.py if present). Merge = `git checkout main && git
merge <branch>` + push + `herdr worktree remove` on clanker side.
