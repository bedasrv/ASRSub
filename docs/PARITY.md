# Rust port parity checklist

**Question this answers:** when is the pre-rewrite Python safe to delete?
**Answer:** the checklist is complete (2026-09-08) — every row is COVERED,
DIVERGED-with-rationale, or explicitly waived. The rewrite-branch `legacy/`
copy was deleted the same day (criterion met); the remaining Python on
`main` was removed 2026-09-09 when the Rust tree landed there. The two
release-contract suites were relocated to `tests/` and stay green.
Reference code survives in git history (`main` history holds the old
root-level files, e.g. `git show <sha>:orchestrator.py`; the `legacy/`
tree survives on the `backup/pc-208-20260907` branch); this file preserves
the *decisions*.

## Method

- Legacy oracle: `legacy/tests/` (45 files, ~293 cases) + `legacy/pipeline/dry_tests.py`
  (109 cases) ≈ **~400 pinned behaviors**, inventoried 2026-09-08.
- Rust coverage **at the time of the port**: 72 unit `#[test]` (incl. the
  full-program sim, 40+ asserts across phases A–I) + 4 binary-boundary
  integration tests + 21 stdlib-unittest release-contract cases. The tree has
  grown since: 195 unit tests + 11 integration tests (`cargo test`; `README.md`
  quotes the same pair) and 61 release-contract cases (`python3 -m unittest
  tests.test_immutable_release_contract tests.test_release_descriptor_execution`).
  This file records the port decision, not the current counts.
- Each row maps one legacy behavior area to its Rust status. Live contracts
  remain `README.md` + `docs/HEALTH.md`; `docs/PLAN.md` is provenance only.

## Legend

| Status | Meaning |
|---|---|
| COVERED | Rust tests pin the same behavior |
| PARTIAL | Core ported, edge cases untested |
| GAP | Behavior exists in Rust but is untested, or unverified |
| DIVERGED | Deliberately changed or removed in the port |
| ABSENT | Legacy feature with no Rust counterpart at all |

## Checklist

| # | Area | Legacy source (cases) | Rust coverage | Status | Notes |
|---|---|---|---|---|---|
| 1 | Release contract (compose/build/deploy/rollback docs) | `test_immutable_release_contract` (18 at the port; 58 today), `test_release_descriptor_execution` (3) | Oracle repaired AND live: fixed stale `legacy/`-relative paths (reorg breakage), evolved 2 Python-era pins to the rewrite contract (no huggingface mount / nvidia runtime — remote-only; `command: [daemon]`); 2026-09-09 evolved again to the GHCR CI contract (CI builds + pushes full-SHA tags, pull-based deploy, `deploy.sh` deleted); **21/21 green at the time of the port; 61 cases today** | COVERED | Resolved 2026-09-08, re-evolved 2026-09-09. Run: `python3 -m unittest tests.test_immutable_release_contract tests.test_release_descriptor_execution`. Extended 2026-09-13 (round 7): the wire-language measurement is evidence, so `tools/probe_wire_langs.py` refuses to overwrite an existing output CSV — non-zero exit naming the path, pointing at `--force`/`--out` — and the refusal is decided before any request is spent; `--force` overwrites and `--recheck`/`--dry-run` never write (five offline cases drive the script against a refused `127.0.0.1` port, and the committed `tools/wire_langs-20260913.csv` is asserted by row count and sha256). |
| 2 | Jimaku client (ranking, 429, cache, AniList) | `test_jimaku_api` (32) | 14 tests in `src/jimaku.rs`: shapes, pick_entry, full rank corpus, cache-key pin, stub-server auth/params/429/snippet/download/anilist-hit-miss-failure | COVERED | Resolved 2026-09-08. Fixed en route: unexpected shape now errors (was silent empty), HTTP errors carry status+snippet, 429 carries reset_after, download uses legacy `.part` naming + cleans up on failure, cache merge writes `fetched_at`. Recorded folds: typed `JimakuRateLimited` → anyhow message (no caller branches); strict `S##E##` tag kept over loose regex (pre-existing, reasoned); `download()` returns `()` (callers use the known dest); `JIMAKU_API_KEY` env fallback lives in config layer. |
| 3 | Jimaku hunt scheduling (budget, backoff, tombstone, state-file) | `test_jimaku_hunt` (43) | Middle path: per-stem 24h retry cooldown (`jimaku_tried` now maps stem→Instant; restart retries everything), predicate unit-tested | COVERED | Resolved 2026-09-08. Decision, not a port: budgets unnecessary (global 500ms pacing guards the rate limit by construction), 429s miss into the next cooldown, tombstones pointless with no upgrade pass (no Sonarr-404 orphans; a hopeless stem costs ~2 calls/day), flat-daily lands uploads within ~24h like the capped ramp did. Rationale recorded on `JIMAKU_RETRY_COOLDOWN`. |
| 4 | Registry core (upsert/delete/reconcile/locking) | reconciliation ×3 files (7), `test_registry_lock_and_tmp_ladder` (3), `test_registry_write_serialization` (1) | `src/state.rs` (5): roundtrip, consume, delete scoping, kind, **concurrent-appends serialization**, verified-targets | COVERED | Resolved 2026-09-08. Hash-trust/reconcile-by-hash obsolete by design (no hashes). Lock upgrade/tmp-ladder semantics replaced by fd-lock + atomic renames (pinned by the concurrency test). |
| 5 | Language aliases/compat (jpn/jp/ind, canonical rows) | adversarial 1–7 + compat + alias_gaps (~30) | `src/lang.rs` (32 today) + delete scoping + `m:/e:` routing + `missing_of` alias test + alias-adoption test (`sidecar_exists` jpn↔ja) + stale-row handling (`verified_targets`) | COVERED | Resolved 2026-09-08. Adversarial ladders that assumed the upgrade/reconcile passes are moot (no such passes); the behaviors that survive — normalization, canonical rows, stale distrust, alias adoption — are each pinned. Extended 2026-09-13: the alias table now covers the ISO-639-2/B and 2/T spellings Whisper needs (`fre`/`fra`→`fr`, `ger`/`deu`→`de`, …) and the full names Sonarr/Radarr report (`"Japanese"`→`ja`), with unknown codes still passing through lowercased/alphanumeric. Extended 2026-09-13 (round 2): a trailing parenthesised/bracketed qualifier is dropped before the alnum collapse, so `"English (US)"`→`en` and `"Chinese (Traditional)"`→`zh` instead of `englishus`/`zhhant`; the table itself is pinned unique and round-tripping, and `identity_accepts` separates a token that names a language from an uncertainty marker (`und`/`unknown`/`""`) while `wire_accepts` is the measured endpoint accept set. Extended 2026-09-13 (round 4): `fil`/`tgl`/`tagalog`/`filipino` → `tl` (the endpoint answers `fil` with HTTP 400 and `tl` with 200, so remapping keeps the reported value, the display name, the sidecar suffix and the translate/skip decision consistent), `normalize_lang` strips a `-XX`/`_XX` subtag when the head names a language (`pt-BR` → `pt`, `fil-PH` → `tl`) and refuses a subtag on a head that names nothing (`zz-ZZ` → `zzzz`, `und-US` → `undus`), and the single predicate is split so narrowing the wire gate can no longer change track identity. Extended 2026-09-13 (round 5): the accept set is re-derived from Whisper's own 100-code language table by a committed live probe (`tools/probe_wire_langs.py`, 118 requests, 100 codes accepted, CSV kept as evidence) — `jw` and `ln` were the two the 98-code set was missing, so a `jw`/`ln`-tagged track lost its pin and left its later chunks unguarded; `jv` (HTTP 400) now remaps to the accepted `jw` like `fil`/`tgl` → `tl`, and both new codes have display names. `sidecar_aliases` is derived from the alias table instead of a three-entry literal, so a canonical rename cannot orphan an older spelling on disk: `{stem}.fil.hi.srt` (written by earlier revisions) is discovered again, and `pt-BR`/`zh-Hant` inherit the whole `pt`/`zh` list. Measured on the committed functions (2026-09-13): `replaceable_target_sidecar_paths` — the non-forced list that adoption, deletion and the dominated scan read (`src/main.rs:750`, `src/episode.rs:526`, `src/actions.rs:229`) — widens 2 → 10 for `tl`/`fil`/`tgl`/`tagalog`/`filipino`, 2 → 6 for `pt`/`pt-BR`/`pt_BR`/`por`, 2 → 8 for `zh`/`zh-Hant`/`chi` and 2 → 4 for `jw`/`jv`; the wider `sidecar_paths` (five flags per spelling, the ladder's existence probe, `src/ladder.rs:57`) widens 5 → 25 for `tl`, 5 → 15 for `pt`, 5 → 20 for `zh`, 5 → 10 for `jw`, 15 → 20 for `ja`/`en` and 10 → 15 for `id`. The "5 → 25" figure belongs to that function, not to the replaceable list (`tl` goes 2 → 10 there). `ja`/`id`/`en` stay strict supersets of what they listed before (`ja` 6 → 8, adding `.japanese[.hi].srt`; `id` 4 → 6, adding `.indonesian[.hi].srt`; `en` 6 → 8, adding `.english[.hi].srt`), and `sidecar_aliases("ja")` is now ordered `ja`, `jp`, `jpn`, `japanese` where round 4's literal was `ja`, `jpn`, `jp` — cosmetic, since no caller is order-sensitive (each iterates the list or uses `.any()`). Extended 2026-09-13 (round 6): `WIRE_ACCEPTED_LANGS` is now `(code, name)` pairs — the code is the wire gate and the second element is Whisper's own name for it — so `normalize_reported_lang` reads a provider's own name for a code out of the same table that decides what may be sent (`known_code` → `wire_name_code`), and a reported spelling in no table is still rejected with the value named. The curated `REPORTED_LANG_SPELLINGS` entries stay for the family spellings Whisper's table does not use (`farsi`, `mandarin`, `castilian`, `flemish`). Extended 2026-09-13 (round 7): the reported vocabulary is widened to the accept set's OWN other standard spellings — a new `REPORTED_LANG_ISO_SPELLINGS` table of the ISO 639-2/B, 639-2/T and 639-1 spellings of every accepted code that has one outside the alias/curated tables (77 entries: `yid`→`yi`, `tib`/`bod`→`bo`, `alb`/`sqi`→`sq`, `baq`/`eus`→`eu`, `ger`/`deu` is already an alias, `jav`→`jw`, `wel`/`cym`→`cy`, `ice`/`isl`→`is`, `arm`/`hye`→`hy`, `geo`/`kat`→`ka`, `mao`/`mri`→`mi`, `mac`/`mkd`→`mk`, `bur`/`mya`→`my`, `slo`/`slk`→`sk`, …), read only through `known_code`/`normalize_reported_lang`. It closes a reversal the round-6 fix created: a tag whose head names the language (`yiddish-x`) pins `yi`, and a provider answering `yid` then failed the episode permanently with `whisper language mismatch: pinned yi, reported yid` (measured with the real binary on `61b8d99`), where the round-5 tree wrote the SRT. Admission rules, each asserted: a spelling resolves to exactly ONE code; it may not equal another code or another code's wire name; every target is in the accept set; the ISO owner is the same language (so a macrolanguage spelling is kept only where the accept set holds exactly one code for it — `pus`→`ps`, `swa`→`sw`, `aze`→`az`, `est`→`et`, `lav`→`lv`, `mlg`→`mg`, `mon`→`mn`, `nep`→`ne`, `san`→`sa`, `uzb`→`uz`, `yid`→`yi` — and `nor` is deliberately left out: it is already a pinning alias for `no` and its macrolanguage spans `no`/`nb`/`nn`); languages whose only ISO spelling is the code or which have none (`haw`, `yue`) contribute nothing rather than an invented entry. The 106 pin aliases and the 52 curated spellings are unchanged and pinned by literal maps, and the new table is reported-only: no bare ISO spelling can name a track from a tag or reach the wire, while detection is widened on purpose (a detected `yid` yields the accepted `yi`, so the follow-up chunks keep a pin). |
| 6 | HI/canonical policy, delete + upgrade sidecars | `test_permanent_subtitle_policy_red` (26), `test_delete_upgrade_sidecar` (19) | HI-twin globs implemented + unit-tested (`replaceable_target_sidecar_paths`); delete path exercised every sim retry phase; hash-trust/retime-gate cases obsolete by design (no hashes, no retime) | COVERED | Resolved 2026-09-08. Upgrade-on-changed-JPN waived: without content hashes there is no change signal, and an adequate ASR sidecar is by definition good enough — a quality nicety, not correctness. Revisit if hashes ever return. |
| 7 | Webhook durability (SQLite ledger, claim/lease/token fencing, auth) | `test_webhook_durability_red` (17), `test_webhook_remediation_tdd` (15), `test_remaining_remediation_tdd` (7) | Auth restored on `/webhook` (either sender header, 401 otherwise) + validate-before-wake + in-flight dedup; ledger intentionally absent (filesystem-as-ledger: sidecars adopted via ladder guards) | COVERED | Resolved 2026-09-08. OPS: Tdarr/Sonarr notification must send the key header. Remaining waiver: lease-fencing/dupe semantics replaced by simpler dedup — recorded here. |
| 8 | Retime/align | `test_retime_safety` (10), dry_tests retime (~25) | INTENTIONALLY REMOVED (`RETIME_*`/`ALIGN_*` keys pruned) | DIVERGED | Signed off 2026-09-08. Rationale: Japanese keeps raw timing (ASR or Jimaku-direct, both already aligned to the video), external trusted as-is; the retime path existed to fix Bazarr-provider timing drift for a different source mix. If mistimed-external reports arrive, revisit — the legacy module is in git history. |
| 9 | Timeline gate (pileup/hole/shrink/monotonicity) | `test_timeline_gate` (8), marker-guard + recalc-order (2) | `src/srt.rs` (5 new): pileup>2, gap>30%, coverage<80%, monotonicity, healthy±duration — legacy thresholds as consts; wired at the episode call site with a once-per-episode duration probe | COVERED | Resolved 2026-09-08. Host-local backup-file case not portable (noted in test). Recalc-order/timeline-recalc cases moot: no recalc pass exists (see row 8 direction). |
| 10 | Translation guards/stitch/fallback/source-language | lirik_fix (4), source-language (5), dry_tests merge/echo/tail/per-line (~8) | `src/translate.rs` (7): guards, stitch order/oversize, prompt source pin, empty-on-persistent-failure, foreign-guard applicability | COVERED | Resolved 2026-09-08. `display_source_lang` extracted (ladder/ASR domain was ja/en only, so the mapping was exact, now pinned). "Fallback preserves source language" meant the `source_lang` parameter propagates through per-line retries (pinned), with `""` output on persistent failure (pinned) — not source-text output. Extended 2026-09-13: the ASR now reaches every tagged language, so `display_source_lang` names each one (`fr`→`French`) instead of calling every non-English source Japanese (pinned), and the foreign-script guard is pinned to Japanese sources only — a French transcript is mostly-latin and the guard would rewrite it to SDH placeholders (pinned). |
| 11 | ASR track selection + embedded sweep roots | `test_embedded_target_languages` (5), `test_embedded_srt_sweep_root` (4) | `src/asr.rs` (37 today): the full `choose_source` table (target/original/ja/untagged-original/en/first-non-commentary/commentary/empty-target), the `fre`-never-becomes-`ja` regression, tags that name no language (`und`/`unknown`/blank/`jajp`) taking the detection path, a tag naming a language the endpoint rejects being carried but never sent, the wire never carrying a rejected code, request plumbing (probe omits `language`, pinned chunks carry it, the guard compares against the value the form holds), detection/contradiction failure, the identity restore for targets outside the old whitelist and the pin-path subtag strip; `src/lang.rs` `original_language` + `src/sonarr.rs` (2) original-language parse (object and bare string); webhook `extract_embedded` + `map_path` pinned; sweep-root chain waived (no sweep feature — webhook paths are sender-provided, `map_path` is the only mapping) | COVERED | Resolved 2026-09-08. Embedded-adoption semantics (canonical outputs, no false missing) hold via `sidecar_exists` replaceable variants, pinned in row 5. Extended 2026-09-13 (defect fix): `choose_source` used to force `ja` for every non-`en` target, so a `fre`+`eng` episode with no Japanese track was transcribed as Japanese and produced a fabricated Indonesian subtitle registered `done`. The tag now picks the track and is the code sent to Whisper; an untagged track is probed unforced and the detected `verbose_json` code pins the rest; an unestablishable source language fails closed (sim Phase H: `failed`, no sidecar, no upload, no registry row). Provenance records `source_lang`/`source_stream` in the registry `extra`. Extended 2026-09-13 (round 2, adversarial follow-up): `detects_language()` tested *absence*, not *usability*, so a present-but-unusable tag (`und` — what muxers write for an unset language — `unknown`, `englishus`, an empty/whitespace tag) was pinned verbatim as Whisper's `language` and the provider answered HTTP 400, failing the episode permanently. Pinning now requires an accepted code; a response value that is not a code at all (a full name the reported-spellings table does not map, a long token) errors, and the message names the value and is distinct from the absent case — a plain 2–3 letter token IS accepted as metadata (`{"language":"xx"}` → `Ok(xx)`), which is why this row's earlier claim that an unusable response code "errors exactly like a missing one" was wrong. `whisper_text_fields` still drops a rejected code even if one is handed over (defense in depth). The untagged-original rule sits between `ja` and `en` — `[und(0), eng(1)]` now transcribes stream 0, not the dub — while the tagged `[jpn, eng]` anime case is unchanged. An empty `--lang` is treated as no target instead of matching a present-but-empty tag. Sim Phase I covers the successful detection run end to end (sidecar written, `source_lang`/`source_stream` rows, no `language` field on the probe; the stub now records received form bodies and can report a detected language). Ladder rows carry `source_lang` but no `source_stream` — the README/DEPLOY wording was corrected to say so. Extended 2026-09-13 (round 4, adversarial follow-up): one predicate served track IDENTITY, the wire gate and the guard, so narrowing the wire set made `[eng(0), cy(1)]` targeting `cy` pick the English dub at stream 0 and pin `en` (transcribing the dub and translating into `cy`) where the parent pinned `cy`, and made the guard abort a 4-piece detection run with `pinned yue, reported en` while no `language` field had ever been sent — a NEW permanent per-episode failure for every detected code outside the 30 (`yue`, `cy`, `haw`, `ta`, `ml`, `tl`, …). Identity is now the round-2 shape rule, the wire gate is the measured 200-set (round 5 re-measured it against Whisper's own 100-code table: 118 requests — 100 candidates + 4 by-design-unreachable spellings + 14 refused controls — with all 100 candidates answering 200 and echoing themselves, so the set is those 100; the round-4 table was that set minus `jw`/`ln` — and HTTP 400 for `ceb`/`eo`/`jv`/`zu`/`fil`/`tgl`/`tam`/`tel`/`slo`/`cat`/`und`/`na`/`zz`), and the guard runs only when a request really carried a pin, comparing against the same value the form holds (`wire_lang`), so a differing report on an unforced request is information rather than a contradiction. The CLI `--lang` gate shares that normalization, so `--lang en-US` is `en`. The detection-only spellings table is the curated extras list and now says so (round 6 moved the per-code names into the accept table, so it is no longer the whole reported vocabulary). Extended 2026-09-13 (round 5): the contradiction guard now covers the whole-file path too — `transcribe_file` never called it, so a 40 s file tagged `eng` could commit an `en`-labelled SRT holding the provider's `ja` text and exit 0 — and it judges a reported value with the reported table (`normalize_reported_lang`), so the 52 legitimate spellings (`tamil` for a `ta` pin, `welsh` for `cy`, `Cantonese` for `yue`) no longer abort an episode; a genuinely different language still fails closed with both codes named. Two committed tests drive the real binary end to end against a localhost stub with fake `ffmpeg`/`ffprobe`: the multi-chunk path asserts the `language` field of each request and that the guard fires only for a pin that was sent, and the single-file path asserts both directions (sent pin contradicted → error and no artifact; nothing sent → any report stands). A table-driven shapes matrix pins (chosen stream, carried code, wire value) for every moved shape, including duplicate tags, commentary/description titles, single-track region spellings and an empty target. Extended 2026-09-13 (round 6): the accept set became the single definition of both halves — `WIRE_ACCEPTED_LANGS` is now `(code, name)` pairs — because 22 of the 100 codes have a Whisper name that neither language table knew (`afrikaans`, `tibetan`, `hawaiian`, `haitian creole`, `javanese`, `nynorsk`, …), so a provider answering a language's OWN name failed a pinned episode permanently with `whisper language mismatch: pinned <code>, reported <name>` (measured on `b0b91e68` with the real binary, one run per code, 22 non-zero exits); the names are read out of that one table (`known_code` → `wire_name_code`) so the accept set and the reported-name vocabulary cannot drift, the curated `REPORTED_LANG_SPELLINGS` keeps only the family spellings Whisper's table does not use, and a spelling in no table is still rejected with the value named. Pinned by name for the 22, table-driven over all 100 on both sides of the guard, and end to end: a single-request run per code and the 22 codes on the piece path must all commit, while a pin contradicted by a DIFFERENT language's name (`tibetan` for an `en` pin) still fails closed with both codes named, and `display_name` is asserted to agree with the reported names wherever it names a code (the `jw`/`ln` gap). Part (2) of the multi-chunk test no longer asserts a request count: the pieces are dispatched concurrently (`try_join_all`) and the abort cancels the requests still in flight, so 1/2/3 is a race — the count is pinned on the runs that COMPLETE (parts (1) and (3)), together with a one-cue-per-piece check, so a run that transcribes only some chunks cannot pass. Extended 2026-09-13 (round 7): the guard and detection read an accepted code's other standard spellings the same way (`yid` to a `yi` pin, `tib`/`bod` to `bo` — `lang::REPORTED_LANG_ISO_SPELLINGS`), pinned by a unit test that drives every entry through `check_pinned_lang` plus the two integration tests below, and two more drive the real binary end to end: a `yiddish-x`-tagged single request whose provider answers `yid` must commit and write the artifact (the measured defect: non-zero exit, no artifact, `pinned yi, reported yid`), and an untagged track whose detection probe answers `yid` must resolve to the accepted `yi` — the two remaining pieces then carry `yi` (before this the token was metadata only, so they were left unforced). |
| 12 | Control API semantics (stale-done, false positives, wanted/library) | stale_done (2), false_positive_paths (3), source_target_registry (1), dry_tests status/library/activity (~8) | Fixed + pinned: `discover` verifies done rows against registry targets on disk (`verified_targets`, unit-tested) for series AND movies — feeds run_pass, wanted, and library together; sim Phase F proves stale-done self-heals end-to-end; Bazarr alias normalization pinned (`missing_of` test) | COVERED | Resolved 2026-09-08. Late-marker pathless-row guard waived: nothing reads the registry for claim decisions (provenance display only), so there is no claim path for a marker to hijack — marker handling is cue-level and tested. |
| 13 | Actions retry/skip round-trip | action_normalization (1), dry_tests actions/state (~8) | Consume unit test + sim Phase C | COVERED | Language scoping (`null` = whole episode, else explicit list) applies to both retry and delete; retries additionally reprocess inline the same pass (resolved from Sonarr/Radarr, skips/exclusions win). |
| 14 | In-memory job queue | `test_job_registry` (9) | No counterpart: loop daemon uses semaphores; dashboard queue widgets degrade gracefully (`data.queue \|\| {}`, empty job list); `current` shows the in-flight pass | DIVERGED | Resolved 2026-09-08. Rationale: the queue existed to schedule webhook workers; with wake+extract inline there is nothing to schedule. |
| 15 | Readiness (media check → not-ready) | `test_readiness` (1) | Adapted: `/status` now carries additive `media_ok` (NAS root present), pinned by a wiring test — a dead mount no longer looks like "idle" | COVERED | Resolved 2026-09-08. Full btrfs/ledger/paused-boot gates belonged to the retired Python boot (no paused boot, no ledger DB here); container healthcheck still hits `/health`. |
| 16 | Glossary v2/knowledge-block/cast | dry_tests glossary/knowledge/cast (~5) | `src/glossary.rs` (3): flat-map migration, few-match fallback, **knowledge-block rendering** (matched cast, aliases, kinds, empty-for-unknown) | COVERED | Resolved 2026-09-08. |
| 17 | Upload edge cases (204 vs differing sidecar, movie retry codes) | upload_collision (1), dry_tests upload (2) | NEW sim Phase G: 2×500→204 delivers exactly 3 attempts and completes; all-500 still completes (sidecar authoritative); shared 204/429/5xx policy covers series+movies identically (legacy movie test asserted exactly this sequence) | COVERED | Resolved 2026-09-08. Post-204 read-back waived: we upload bytes just written to the canonical sidecar, so there is no legacy-alias collision state to detect; staleness is caught at `discover` instead (row 12). Misleading `bazarr.rs` doc fixed to match. |
| 18 | Movies end-to-end | Scattered (~10): candidates, library, fileless recovery | NEW sim Phase E: Radarr movie discovered/transcribed/uploaded, `kind: movie` state+registry rows, series/movie id-100 collision proven | COVERED | Resolved 2026-09-08. Fileless-movie recovery has no counterpart (no sweep feature — see row 11); movie upload retry codes folded into the shared 204/429/5xx policy (see row 17). |
| 19 | Regen candidate pass | dry_tests regen (~8) | No counterpart — intentionally absent (`config.rs` documents `REGEN/` as dropped); the `retry` action covers the need (clears state + deletes sidecars → Bazarr re-wants → reprocessed) | DIVERGED | Resolved 2026-09-08. |
| 20 | Benchmarks/eval harness | `legacy/benchmarks/` (7 scripts) | No equivalent | DIVERGED | Decided 2026-09-08: waived as a parity item. The harness scores LOCAL models (faster-whisper, NLLB, COMET, Gemma FLORES) that cannot run in this deployment (no GPU, remote-only by design). Remote-provider quality monitoring is an unbuilt capability, not a port gap — file it as new-feature work if wanted, not here. |

## Port queue (highest value first)

1. Row 7 — verify webhook auth/dedup threat model; port or fix. ✅ done 2026-09-08
2. Row 2 — port `test_jimaku_api` ranking/error/cache cases to `src/jimaku.rs`. ✅ done 2026-09-08
3. Row 3 — decision: accept hunt simplification (waive with rationale) or re-add backoff. ✅ done 2026-09-08 (middle path: 24h retry cooldown)
4. Row 18 — movie phase in `src/sim.rs`. ✅ done 2026-09-08 (Phase E + stale-heal Phase F)
5. Rows 9, 12, 10 — small pure-function ports (timeline gate, API semantics, source-lang). ✅ done 2026-09-08
6. Rows 6, 17 — `actions.rs`/`episode.rs` edge tests (HI globs, upgrades, upload guards). ✅ done 2026-09-08 (sim Phase G; upgrades + read-back waived with rationale)
7. Rows 19, 14, 4-locking, 11-roots, 15, 16 — verify-and-waive or port. ✅ done 2026-09-08
8. Rows 1, 8, 20 — sign-off decisions (release assertions, retime removal, eval strategy). ✅ done 2026-09-08

## Deletion criterion for the pre-rewrite Python — MET 2026-09-08, deleted same day; `main` cleared 2026-09-09

~~Delete the working-tree copy when~~ every row above is COVERED,
DIVERGED-with-rationale, or explicitly waived — and the waivers are recorded
in this file. Git history preserves the code regardless (`main` history
holds the old root-level files; the `legacy/` tree is on the
`backup/pc-208-20260907` branch); this file preserves the *decisions*. The
release-contract oracle moved to `tests/test_immutable_release_contract.py`
+ `tests/test_release_descriptor_execution.py` (run: `python3 -m unittest
tests.test_immutable_release_contract tests.test_release_descriptor_execution`).
