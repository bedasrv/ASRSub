# Rust
/target/
**/*.rs.bk

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
venv/
env/

# Secrets / environment files
.env
*.env
!*.env.example
!.release.env
!release.env
!release.json
pipeline.env
# Live provider keys: copy asrsub_providers.json.example and fill in.
# The real file must never be committed (purged from history 2026-09-08).
asrsub_providers.json
!asrsub_providers.json.example
*.key
*.pem

# Runtime state & data
*.lock
# ...but keep the Rust dependency lockfile (binary crate: reproducible builds)
!Cargo.lock
state.jsonl
state*.jsonl
tmp/
*.log

# Local config
config.local.py
config.local.yaml

# Media / data artifacts
episodes/
ground_truth/
outputs/
*.wav
*.mp3
*.srt

# OS / editor cruft
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Systemd / deployment local files
*.service

# Eval harness local data (downloaded corpora, wavs, hf cache)
benchmarks/data/
benchmarks/results/*.sys.txt
